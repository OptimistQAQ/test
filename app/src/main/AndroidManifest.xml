<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/main"
        android:label="@string/app_name"
        android:roundIcon="@drawable/main"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.sqllitedemo.SqlLiteDemoActivity"></activity>
        <activity android:name=".activity.data.SharedPreferencesActivity" />
        <activity android:name=".activity.chaundi.CaculateActivity" />
        <activity android:name=".activity.chaundi.ChuandiActivity" />
        <activity android:name=".activity.url.UrlActivity" />
        <activity
            android:name=".activity.UserInfoActivity"
            android:label="@string/app_name" />
        <activity android:name=".activity.CaipinActivity" />
        <activity android:name=".activity.OrderedActivity" />
        <activity
            android:name=".activity.RegisterActivity"
            android:label="@string/app_name" />
        <activity android:name=".activity.OrderFoodActivity" />
        <activity android:name=".activity.TextActivity" />
        <activity android:name=".activity.TabhostActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>